<div class="card card-compact <%= 'lg:col-span-2' if wide %> col-span-1 p-4 shadow-xl rounded-md p-8 mx-4 lg:mx-0 bg-base-200 ">
  <div class="flex justify-between">
    <h2 class="card-title"><%= title %></h2>
    <div class="flex self-center gap-3">
      <%= display_links %>
    </div>
  </div>

  <%= statistic_display %>
  <div class="flex justify-center">
    <%= details_cta %>
  </div>
</div>
